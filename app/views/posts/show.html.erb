<h2><%= @post.title %></h2>
<p><%= @post.body %> </p>
<%= image_tag @post.image %>

<%= link_to "Edit post", edit_post_path(@post) %>
<%= link_to "Back", root_path %>
<%= link_to "Delete", post_path(@post), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete your post?"} %>